definitions:        # Schema defination for request body
  index:
    type: object
    properties:
      Message:
        type: string

paths:
  /:
    get:
      tags:               # Tag property
        - Index         # Value of the tag
      summary: Returns A Welcome Message
      responses:
        '200':
          description: A JSON Array Of The
          content:
            application/json:
              schema: 
                $ref: '#/definitions/index'
